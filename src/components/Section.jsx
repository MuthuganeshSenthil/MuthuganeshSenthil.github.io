import { motion } from "framer-motion";
export function Reveal({children,delay=0,className=""}){return <motion.div initial={{opacity:0,y:16}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:"-60px"}} transition={{duration:0.5,delay,ease:"easeOut"}} className={className}>{children}</motion.div>;}
export default function Section({id,index,label,title,kicker,children,className=""}){return <section id={id} className={`border-b border-line ${className}`}><div className="max-w-6xl mx-auto px-4 md:px-8 py-16 md:py-24"><Reveal><p className="font-mono text-xs text-cool tracking-widest mb-3">[{index}] {label}</p><h2 className="font-display text-3xl md:text-4xl font-semibold mb-2">{title}</h2>{kicker&&<p className="text-ink-dim max-w-xl mb-10">{kicker}</p>}{!kicker&&<div className="mb-10"/>}</Reveal>{children}</div></section>;}
