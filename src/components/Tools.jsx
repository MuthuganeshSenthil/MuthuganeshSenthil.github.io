import Section,{Reveal} from "./Section";
import {toolGroups} from "../data/tools";
export default function Tools(){return <Section id="tools" index="04" label="TOOLS.env" title="Tools" kicker="Grouped by category. Filled tags are tools I actively use; outlined tags are ones I'm currently learning." className="bg-bg-panel/40"><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-8">{toolGroups.map((group,gi)=><Reveal key={group.category} delay={gi*0.06}><h3 className="font-mono text-xs text-ink-faint tracking-widest mb-3">{group.category.toUpperCase()}</h3><div className="flex flex-wrap gap-2">{group.tools.map(t=><span key={t.name} className={t.confirmed?"px-3 py-1.5 rounded-md text-xs font-mono bg-signal/10 border border-signal-dim text-signal":"px-3 py-1.5 rounded-md text-xs font-mono border border-line-bright text-ink-faint"}>{t.name}</span>)}</div></Reveal>)}</div></Section>;}
